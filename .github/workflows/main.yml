on: 
  push:
    branches: dev

jobs:
  main_workflow:
    uses: DjNaGuRo-R2/gh-reusableWorkflows/.github/workflows/gh-cargo_doc.yml@main
    with:
      ONLY_LIB: true
      RELEASE_MODE: true
      PROJECT_ROOT: ./cargo_toml
      
